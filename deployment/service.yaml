apiVersion: v1
kind: Service
metadata:
  name: cat-dog-classification-api-service
spec:
  type: NodePort
  selector:
    app: cat-dog-classification-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000